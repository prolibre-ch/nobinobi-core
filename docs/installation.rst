============
Installation
============

At the command line::

    $ easy_install nobinobi-core

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv nobinobi-core
    $ pip install nobinobi-core
